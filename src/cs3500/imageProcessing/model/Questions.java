package cs3500.imageProcessing.model;

public class Questions {

  /*
  - There are a few different things to consider when building the model.
    - Image format: there can be many different image formats with PPM just being one of the formats
    we are working with. This begs the question of what the similarities are between all of the image
    formats. It was said that one of the commonalities between all images
    - Should the image instance contain a hashmap that stores the same image filtered a few times?
   */

  /*
  - TODO:
    - It seems like we should return a new image after processing it. (maybe even keeping the old image)
    - if you have a map, consider coming up with an id generator.
   */
}
